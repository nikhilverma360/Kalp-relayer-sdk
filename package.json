{
  "name": "kalp-relayer-monorepo",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "sdk",
    "examples/*"
  ],
  "scripts": {
    "build": "npm run build -w sdk",
    "lint": "npm run lint -w sdk",
    "dev:example": "npm run dev -w examples/basic"
  }
}

