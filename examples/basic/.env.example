# Kalp Relayer SDK Example Configuration

# RPC endpoint for the blockchain (required)
# Example for Sepolia: https://sepolia.infura.io/v3/YOUR_INFURA_KEY
RPC_URL=

# Private key for signing (required for this example - DO NOT commit real keys!)
# Example: 0x1234567890abcdef...
PRIVATE_KEY=

# Kalp Relayer contract address (required)
# This is the address of the deployed Kalp Relayer smart contract
RELAYER_ADDRESS=

# Sponsor address (required)
# Address that will sponsor the gas fees
SPONSOR_ADDRESS=

# Optional: Domain name for EIP-712 signing (default: KalpRelay)
DOMAIN_NAME=KalpRelay

# Optional: Domain version for EIP-712 signing (default: 1)
DOMAIN_VERSION=1

# Optional: Kalp Relay API endpoint (default: https://alpha-wallet-api.kalp.studio/relayer/relay)
RELAY_API_URL=https://alpha-wallet-api.kalp.studio/relayer/relay

# Kalp API Key for authentication (required)
# Get this from your Kalp Studio account
API_KEY=YOUR_API_KEY_HERE

# Optional: Target contract to interact with
# Example contract with increment() function
TARGET_CONTRACT=0x15CeE1b5Eb18e5FC8884862D66812B1BfB7D0e6c

# ========================================
# Token Transfer Example Configuration
# ========================================

# ERC20 token address with EIP-2612 permit support (required for token transfer)
# Example: USDC, DAI, or any ERC20 with permit() function
TOKEN_ADDRESS=

# ERC20Facilitator contract address (required for token transfer)
# This is the deployed ERC20Facilitator.sol contract
FACILITATOR_ADDRESS=

# Recipient address for token transfer (required for token transfer)
# Where the tokens should be sent
RECIPIENT_ADDRESS=

# Amount to transfer in smallest unit (required for token transfer)
# For 18 decimal tokens: 1 token = 1000000000000000000 wei
# Example: 1000000000000000000 (1 token with 18 decimals)
TOKEN_AMOUNT=
